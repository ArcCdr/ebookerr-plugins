# Staged first-party plugins

First-party ebookerr plugins are staged here during 2.20.0, one folder per plugin
(`plugins/<id>/`: `manifest.toml`, `entrypoint.py`, the `<id>` package and its `tests/`), until they
move to their own repository.
